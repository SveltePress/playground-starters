---
title: Search
---

Local Search (Pagefind) is on by default. In this Hosted editor it is **Degraded**: the index is a production-build artifact, so `pnpm dev` shows a notice instead of hits.

Docsearch and Meilisearch are **Observation-only**. See the commented options in `vite.config.js` (this Entry's Focused file).
