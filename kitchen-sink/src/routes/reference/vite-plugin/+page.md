---
title: Vite plugin
---

Edit `sveltepress({...})` in `vite.config.ts`. That is **author-success**.

`llms` and `addInspect` are **Observation-only** — inspect the options, do not expect a live inspect UI or generated `llms.txt` in `pnpm dev`.
