---
title: 默认主题中的 Frontmatter
sidebar: true
---

共享字段：`title`、`description`、`lastUpdate`。

主题特定字段：

- `sidebar` — 是否显示侧边栏（默认 `true`）
- `header` — 是否显示顶栏（默认 `true`）
- `layout` — 是否使用主题页面布局（默认 `true`）

本页的 frontmatter：

```md live
<pre>
{JSON.stringify(fm, null, 2)}
</pre>
```
