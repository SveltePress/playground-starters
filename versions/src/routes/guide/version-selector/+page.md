---
title: Version selector
order: 2
---

The Default Theme adds a version selector automatically when `sveltepress.versions.json` is present.

This page exists only in **1.1**, so the sidebar and page title show a **New** badge while you browse the current version. Switch to **1.0** and the page is gone from that snapshot.
