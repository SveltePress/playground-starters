---
title: Frontmatter in default theme
sidebar: true
---

Shared fields: `title`, `description`, `lastUpdate`.

Theme-specific fields:

- `sidebar` — show the sidebar (default `true`)
- `header` — show the header (default `true`)
- `layout` — use the theme page layout (default `true`)

This page's frontmatter:

```md live
<pre>
{JSON.stringify(fm, null, 2)}
</pre>
```
